using AgendaChallenger.Entities;
using Microsoft.AspNetCore.Identity;

namespace AgendaChallenger.Authorization
{
    //internal sealed class LoginSistema(ApplicationDbContext dbContext, PasswordHasher password)
    //{
    //    public sealed record Request(string Id, string Nome);
    //    public async Task<Sistema> Handle(Request request) {
    //    Sistema? sistema = await dbContext.
        
    //    }
    //}

    internal sealed class LoginSistema
    {        
    }
}
